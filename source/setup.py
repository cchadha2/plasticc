from setuptools import setup

setup(name='data_process',
      version='0.1',
      description='Process data in format required for competition',
      packages=['data_process'],
      zip_safe=False)